#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 * @EXIT_SUCCESS: the returned value
 * Return: Always EXIT_SUCCESS.
 */
int main(void)
{
	printf("%d\n", _strlen("My Dyn Lib"));
	return (EXIT_SUCCESS);
}
